# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
/dist/*
dist-ssr
# Email-specific dist is committed for traceability — Vite-built dist remains ignored
!/dist/emails/
*.local
.env*
# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri signing keys (keep private key secret!)
src-tauri/private.key

# Local release artifacts (built by release.ps1)
artifacts/

# Auto Claude data directory
.auto-claude/
.mcp.json

.design-handoff/

# Auto Claude generated files
.auto-claude-security.json
.auto-claude-status
.claude_settings.json
.worktrees/
.security-key
logs/security/

# Deno lockfile (Deno 2.x format incompatible with Deno 1.x used in CI)
deno.lock
