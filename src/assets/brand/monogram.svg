<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" fill="none">
  <!-- Monogramme Lexena : usage tray, favicon, contextes réduits -->
  <!-- Fond arrondi Signal Green -->
  <rect width="100" height="100" rx="22" fill="#1D9E75"/>
  <!-- Lettre L (barre verticale) -->
  <rect x="28" y="22" width="10" height="50" rx="3" fill="white"/>
  <!-- Lettre L (barre horizontale) -->
  <rect x="28" y="62" width="42" height="10" rx="3" fill="white"/>
  <!-- Pastille accent -->
  <circle cx="70" cy="24" r="7" fill="#3FBFA0"/>
</svg>
