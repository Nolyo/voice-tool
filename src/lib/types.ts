/**
 * Shared types used across features.
 *
 * Populated incrementally during the frontend refactor. For now this file acts
 * as a placeholder; Phase 3 will move Dashboard's `TranscriptionInvokeResult`
 * and `RecordingResult` here.
 */

export {};
